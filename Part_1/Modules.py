'''
A module in Python is simply a file that contains
Python code — like functions, variables, 
or classes — which you can reuse in other programs by importing it.
'''
import math
print(math.sqrt(25))
print(math.pi)
